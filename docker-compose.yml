version: "3.9"
services:
  codeautrix:
    build: .
    ports:
      - "80:8000"
      - "8000:8000"
    volumes:
      - ./backend/storage:/app/backend/storage
    environment:
      - UVICORN_RELOAD=false
